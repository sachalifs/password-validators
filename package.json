{
  "name": "password-validators-monorepo",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w password-validators-demo",
    "build": "npm run build -w @slifszyc/password-validators && npm run build -w password-validators-demo",
    "test": "npm run test -w @slifszyc/password-validators",
    "lint": "eslint ."
  },
  "devDependencies": {
    "eslint": "^9.22.0",
    "typescript": "~5.7.2"
  }
}
